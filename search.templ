package main

templ search(query string, results []string) {
	<header>
		<nav><a href="/">Home</a></nav>
		<h1>Search</h1>
	</header>
	<section>
		@SearchBar(query)
		if len(results) > 0 {
			<ul>
				for _, item := range results {
					<li>{ item }</li>
				}
			</ul>
		}
	</section>
}
